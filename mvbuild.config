#!/bin/bash

tomcat_home="usr/local/tomcat"
workspace_home="~/Workspaces"

# Map all project short names to the paths within Tomcat home directory and the Workspace directory
# These paths are unique to each user